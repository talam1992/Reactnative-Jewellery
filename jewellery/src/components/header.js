//import a library to help make a component
import React from 'react';
import {Text} from 'react-native';

//Make a component
const Header = () => {
	return <Text>Jewellery</Text>
};

//Make the component available to other parts of the app
export default Header;